To run the script.py file first you need to install python 3.7 above
then in the present folder open command prompt (you can type and enter 'cmd' in address bar to open cmd in that folder)
the run this command in command prompt 'pip -r requirements.txt' (this will install required packages)
after that you can run script.py file